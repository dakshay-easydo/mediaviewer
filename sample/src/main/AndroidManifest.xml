<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:ignore="DataExtractionRules,GoogleAppIndexingWarning">

        <activity
            android:name=".features.main.MainActivity"
            android:exported="true"
            android:theme="@style/AppThemeNoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".features.demo.grid.PostersGridDemoActivity"
            android:label="@string/action_images_grid" />

        <activity
            android:name=".features.demo.scroll.ScrollingImagesDemoActivity"
            android:label="@string/action_scroll" />

        <activity
            android:name=".features.demo.styled.StylingDemoActivity"
            android:label="@string/action_style" />

        <activity
            android:name=".features.demo.rotation.RotationDemoActivity"
            android:label="@string/action_rotation" />

    </application>

</manifest>